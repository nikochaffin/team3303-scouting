<%= f.label field.name, field.name %>
<% field.options.each do |option| %>
  <input id="scoring_entry_properties_<%= field.name.parameterize + "_" + option.parameterize %>" type="radio" name="scoring_entry[properties][<%= field.name %>]" value="<%= option %>">
  <%= f.label field.name.parameterize + "_" + option.parameterize, option.humanize %>
<% end %>
